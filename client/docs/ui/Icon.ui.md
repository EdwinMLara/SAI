# Icon Component

## Structure

- `name` (string): Icon name from `react-icons` (e.g., "FaCheckCircle").
- `size` (optional, number): Size in px.

## How to use

```
<Icon name="FaCheckCircle" size={32} />
```
