# Button Component

## Structure

- `variant`: "primary", "secondary", or "ghost".
- `disabled` (optional): Disables the button.
- `children`: Text or icon inside the button.

## How to use

```
<Button variant="primary">Save</Button>
<Button variant="ghost" disabled>Read Only</Button>
```
