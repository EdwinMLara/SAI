# Client

Contains the frontend of the SAI system, including UI components and client-side logic.

## Structure

(...)

## Setup

Run `npm install` to install dependencies. Use `npm start` to run the development server.

## Notes

Ensure API endpoint URLs are correctly set in the environment variables.
